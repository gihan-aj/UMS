namespace Mediator
{
    /// <summary>
    /// Marker interface to represent a notification message.
    /// Notifications are events that have occurred and can be handled by multiple handlers.
    /// </summary>
    public interface INotification
    {
        // This interface is a marker and doesn't need members for now.
    }
}
